export default {
  // We use 0.502 here instead of just 0.5 because the pixels
  // appear a little broken when using 0.5. I guess it's not
  // lining up correctly to the pixel grid
  TEXTCENTER: new Phaser.Point(0.502, 0.502)
}
